//Check if the string contains only digit
//logic is to find first non digit
//divide string in two and check
//or second way is to match with regex pattern
const findNonDigitString = (str) => {
    for(let i=0;i< str.length/2; i++){
        if()
    }
}

findNonDigitString("amit")